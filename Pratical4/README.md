# Practical4 — Simple Shopping App (React)

This is a small demo React app showing:
- Login / Logout (simple email-based login)
- Browse products and Add to Cart
- View cart, change quantities, remove items
- Checkout with a mock payment form

Run locally:

```bash
cd Pratical4
npm install
npm run dev
```

Open the URL shown by Vite (usually http://localhost:5173).
